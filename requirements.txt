# PEINN — research code dependencies (curated for reproduction).
# The operational PEA-OS extras (GUI, messaging, scheduler) are intentionally omitted.

# --- Core ---
pydantic>=2.0
pydantic-settings>=2.0
pyyaml>=6.0
numpy>=1.24

# --- Model / training ---
torch>=2.0
transformers>=4.40            # DeBERTa-v3 backbone for the structured-threat encoder
sentence-transformers>=2.2    # frozen sentence embeddings (no LLM at inference)
scikit-learn>=1.3             # metrics (AUC/MAE) for head training/eval
accelerate>=0.26              # device_map= for HF base-model loading (backends/hf_backend.py)
bitsandbytes>=0.43            # 4-bit load for the logit-lens / NLA interpretability drivers
datasets>=2.16               # HF datasets, needed only for checkpoint regeneration (see docs/REGENERATE_CHECKPOINTS.md)

# --- PEINN RAG (optional; degrades gracefully if absent) ---
faiss-cpu>=1.7                # memory-bank retrieval in core/memory_bank.py (use faiss-gpu for GPU)

# --- Offline LLM labeling / base-model serving (OpenAI-compatible, e.g. Ollama/vLLM) ---
openai>=1.0

# --- Baseline defense (NeMo Guardrails arm) ---
nemoguardrails==0.10.0

# --- Figures / analysis (optional) ---
pandas>=2.0
matplotlib>=3.7
